[package]
name = "pocket-pi-embedded-core"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Allocation-free device state shared by Pocket Pi embedded firmware and host tests"

[lib]
path = "src/lib.rs"

[dependencies]
