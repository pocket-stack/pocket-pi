{
  "name": "pocket-pi-guest",
  "private": true,
  "description": "Build inputs for Pocket Pi's guest bundle. pi's agent core is a real, unmodified upstream dependency here — sync it with `npm update` and rebuild with `node build.mjs`.",
  "type": "module",
  "scripts": {
    "build": "node build.mjs"
  },
  "dependencies": {
    "@earendil-works/pi-agent-core": "0.81.1",
    "@earendil-works/pi-ai": "0.81.1",
    "@earendil-works/pi-coding-agent": "0.81.1",
    "@google/genai": "^2.12.0",
    "@mistralai/mistralai": "^2.5.0",
    "@opentelemetry/api": "^1.9.1",
    "@opentelemetry/api-logs": "^0.220.0"
  },
  "devDependencies": {
    "esbuild": "0.24.2",
    "typescript": "5.7.2"
  }
}
